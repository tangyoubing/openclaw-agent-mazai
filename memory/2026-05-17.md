# 2026-05-17 操作日志

## GitHub 流畅连接研究

### 网络诊断结果
- ICMP ping github.com → 通（74ms，IP 20.205.243.166）
- TCP 443 (HTTPS) → 被 GFW RST 阻断
- TCP 22 (SSH) → 畅通

### 结论
HTTPS 端口被墙，但 SSH 端口可用，最优方案是切 SSH 协议。

### 已完成
1. 生成 RSA 4096 SSH Key（`C:\Users\admin\.ssh\id_rsa`）
2. Git remote origin 已改为 `git@github.com:tangyoubing/openclaw-agent-mazai.git`

### 待完成
- 老板需手动添加 SSH 公钥到 GitHub（https://github.com/settings/keys）
- 添加后测试 `ssh -T git@github.com` 验证连通性
